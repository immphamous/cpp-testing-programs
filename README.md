## bf.cpp
An interpreter for brainfuck. The code that the program executes is hardcoded in, but reading from files or getting console input shouldn't be a hard change.

## bunnies.cpp
A test of random numbers, setting console cursor position, sleeping, and header files. Uses `console.h`. I am unaware how bad it is setting the namespace to std and I will research later on how bad this is.

## keys.cpp
A simple one-key press reader. Uses `stdcin.h`. Same precedent with `console.h`.

## rps.cpp
First program I've written. A simple rock paper scissors program.

## stars.cpp
A simple test program I write when testing out my own and other languages. Originally learned of the program in [BASIC](https://en.wikipedia.org/wiki/BASIC).
